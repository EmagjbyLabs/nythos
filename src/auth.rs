//! Authentication domain concepts and orchestration services.
//!
//! This module will contain all types such as `PasswordHash`, `Claims`,
//! `AccessToken`, and auth flow services.

/// Placeholder marker for the auth module.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct AuthMarker;
